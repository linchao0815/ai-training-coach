{
  "mcpServers": {
    "coros": {
      "command": "coros-mcp",
      "args": ["serve"]
    },
    "gdrive": {
      "command": "uvx",
      "args": ["workspace-mcp", "--tools", "drive", "--tool-tier", "core"],
      "env": {
        "GOOGLE_OAUTH_CLIENT_ID": "${GOOGLE_OAUTH_CLIENT_ID}",
        "GOOGLE_OAUTH_CLIENT_SECRET": "${GOOGLE_OAUTH_CLIENT_SECRET}",
        "OAUTHLIB_INSECURE_TRANSPORT": "1"
      }
    }
  }
}
